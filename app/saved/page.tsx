export const metadata = {
  title: "Saved Items",
};

export default function SavedPage() {
  return (
    <div className="mx-auto max-w-md px-4 py-16 text-center sm:px-6">
      <h1 className="text-3xl font-black uppercase tracking-tight">Saved Items</h1>
      <p className="mt-3 text-sm text-muted">
        Save-for-later is coming soon. For now, use the heart icon on any product to mark it —
        full saved lists arrive in the next update.
      </p>
    </div>
  );
}
